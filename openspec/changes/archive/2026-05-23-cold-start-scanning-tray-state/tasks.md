## 1. 冷启动状态建模

- [x] 1.1 在 Rust 共享状态中增加冷启动完成状态，确保 tray click handler 和 tray title 更新逻辑都能读取
- [x] 1.2 在 Watcher 冷启动开始、无启用 Agent、全部 Agent 完成时正确更新状态
- [x] 1.3 冷启动完成后主动查询今日汇总，并刷新主托盘 token title

## 2. 主托盘扫描态

- [x] 2.1 创建主托盘图标时，将初始 title 设置为当前语言对应的扫描中文案
- [x] 2.2 在冷启动完成前阻止写线程用部分 token 汇总覆盖主托盘扫描态
- [x] 2.3 保持 Provider usage 菜单栏附加显示逻辑不变，不把账号用量纳入本次扫描态控制

## 3. 托盘点击行为

- [x] 3.1 在主托盘左键事件中检查冷启动状态，未完成时直接返回，不展示 Popup
- [x] 3.2 确认右键菜单、Settings 打开和 Quit 行为不受冷启动状态影响
- [x] 3.3 冷启动完成后恢复既有 `toggle_popup_window` 行为

## 4. 前端与文案清理

- [x] 4.1 增加或复用英文 `Scanning...` 与中文 `扫描中...` 文案来源，保持 i18n 一致
- [x] 4.2 清理 Popup 冷启动提示中与新交互冲突或不可达的逻辑，避免展示未完成数据状态

## 5. 验证

- [x] 5.1 增加 Rust 单元测试覆盖扫描文案选择、冷启动完成状态与 tray title 更新门控
- [x] 5.2 增加或更新前端测试，确认冷启动期间 Popup 不再承担用户可见加载状态
- [x] 5.3 运行 `cargo test` 和 `npm test`，确认现有功能不回退
